# UI Design Principle

## 基本原則

- 原則として、UI構築には `shadcn` コンポーネントを使用すること。
- フォント、色および角丸の設定に関しては、`app.cs` に定義されているものを使用し、`shadcn` コンポーネントとの整合性を保つこと。
- 対象デバイスは `PC`, `タブレット` および `スマートフォン` なので、レスポンシブデザインを徹底すること。
